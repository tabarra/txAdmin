export enum TxConfigState {
    Unkown = 'unknown',
    Setup = 'setup',
    Deployer = 'deployer',
    Ready = 'ready',
}

export enum DiscordBotStatus {
    Disabled,
    Starting,
    Ready,
    Error,
}
